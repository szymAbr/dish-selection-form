import FormLabel from "@mui/material/FormLabel";
import styled from "@emotion/styled";

export const FormLabelElement = styled(FormLabel)`
  font-size: 1.5rem;
  display: inline-block;
  padding-bottom: 1.5rem;
`;
